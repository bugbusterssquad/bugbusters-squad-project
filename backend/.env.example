# Database credentials
DB_USER=root
DB_PASS=your_db_password

# Frontend URL(s) for CORS (comma-separated if multiple)
API_ADDR=http://localhost:5173

# JWT settings
JWT_ISSUER=BugBusters
JWT_AUDIENCE=BugBusters
JWT_SECRET=change-me-in-production-please-32chars-min
JWT_EXP_MINUTES=120

# Migration toggle (set false if you apply database_creation.sql manually)
MIGRATE_ON_STARTUP=true

# Optional notification webhook (email/push bridge)
NOTIFICATION_WEBHOOK_URL=
